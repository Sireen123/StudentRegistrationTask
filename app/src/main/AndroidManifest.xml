<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed for saving PDF to Downloads (Android 9 and below) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Optional: read signature/screenshots -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentRegistration">

        <!-- 1. Acknowledgement Screen -->
        <activity
            android:name=".AcknowledgementActivity"
            android:exported="false" />

        <!-- 2. Certificate Activity (Fragments + Logout + PDF Download) -->
        <activity
            android:name=".CertificateActivity"
            android:exported="false"
            android:theme="@style/NoAnimTheme" />

        <!-- 3. Loading Screen -->
        <activity
            android:name=".LoadingActivity"
            android:exported="false"
            android:theme="@style/Theme.StudentReg.Splash" />

        <!-- 4. Student Tasks Screen -->
        <activity
            android:name=".StudentTaskActivity"
            android:exported="true" />

        <!-- 5. Fees List -->
        <activity
            android:name=".FeesListActivity"
            android:exported="false" />

        <!-- 6. Login (Launcher Activity) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.StudentRegistration">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- 7. Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 8. Details Activity -->
        <activity
            android:name=".DetailsActivity"
            android:exported="false" />

        <!-- 9. Course Details -->
        <activity
            android:name=".CourseDetailsActivity"
            android:exported="false" />

        <!-- 10. FileProvider (Needed for signature + PDF sharing) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>